# Build status

Prototype UI and launch-plan data are complete on the `launch-control-app` branch.

Current mode: local browser persistence for safe review.

To make assignments and submissions shared across people/devices, connect a dedicated Supabase project and apply `launch_control_schema.sql`.
