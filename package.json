{
  "name": "nutrition-fitness-website",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Nutrition.Fitness multi-page website and subscription shop preview",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "build": "node scripts/build.mjs && node scripts/build-knowledge-centre.mjs",
    "dev": "npm run build && node scripts/serve.mjs",
    "start": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "npm run test && npm run build && node scripts/check-links.mjs",
    "review": "npm run build && node scripts/make-review.mjs"
  }
}
