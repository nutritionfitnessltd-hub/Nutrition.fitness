{
  "name": "nutrition-fitness-website",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "description": "Nutrition.Fitness launch website, source-backed recipe planner and guarded membership integrations",
  "engines": {"node": "22.x"},
  "scripts": {
    "prebuild": "node scripts/fetch-photo-assets.mjs",
    "build": "node scripts/build.mjs && node scripts/build-launch.mjs && node scripts/build-knowledge-centre.mjs",
    "dev": "npm run build && node scripts/serve.mjs",
    "start": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "npm run test && npm run build && node scripts/check-links.mjs && node scripts/check-launch-banner.mjs",
    "review": "npm run build && node scripts/make-review.mjs"
  }
}
