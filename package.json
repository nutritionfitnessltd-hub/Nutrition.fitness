{
  "name": "nutrition-fitness-complete-site",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-knowledge-centre.mjs && node scripts/verify.mjs",
    "test": "node --test tests/*.test.mjs",
    "preview": "npx serve dist"
  }
}
